import 'package:flutter/material.dart';

class HuaweiPage extends StatelessWidget {
  const HuaweiPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("HELLO new Huawei"),),
    );
  }
}
